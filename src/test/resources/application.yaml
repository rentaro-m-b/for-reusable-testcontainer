spring:
  application:
    name: demo-for-showing-test-containers
  datasource:
    url: jdbc:mysql://localhost:33006/main
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: false
    locations: classpath:db/migration
